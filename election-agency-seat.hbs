{{!< default}}

<main class="gh-main">
  <section class="gh-content gh-canvas">
    {{#get "pages" filter="slug:election-agency-seat-{{agency}}-{{seat}}" limit="1"}}
            {{#foreach pages}}
                {{{content}}}
            {{/foreach}}
        {{else}}
            {{#get "pages" filter="slug:election-agency-seat" limit="1"}}
      
      
      
      
                {{#foreach pages}}
                    {{{content}}}
                {{/foreach}}




    {{else}}
      <h1>Elections - {{seat}} ({{agency}})</h1>
      <p>Historical election results for this seat.</p>
      <p>
        <em
          >Configure by creating a Page with slug: <code>election-agency-seat</code> or
          <code>election-agency-seat-{{agency}}-{{seat}}</code></em
        >
      </p>
    {{/get}}
        {{/get}}
  </section>

  <section class="seat-elections">
    <div class="gh-canvas">
      <div id="election-seat-app" data-agency="{{agency}}" data-seat="{{seat}}">
        <p>Seat election history loads here</p>
      </div>
    </div>
  </section>
</main>
