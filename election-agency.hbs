{{!< default}}

<main class="gh-main">
  <section class="gh-content gh-canvas">
    {{#get "pages" filter="slug:election-agency-{{agency}}" limit="1"}}
            {{#foreach pages}}
                {{{content}}}
            {{/foreach}}
        {{else}}
            {{#get "pages" filter="slug:election-agency" limit="1"}}
      
      
      
      
                {{#foreach pages}}
                    {{{content}}}
                {{/foreach}}




    {{else}}
      <h1>Elections - {{agency}}</h1>
      <p>Election results for this agency.</p>
      <p>
        <em
          >Configure by creating a Page with slug: <code>election-agency</code> or
          <code>election-agency-{{agency}}</code></em
        >
      </p>
    {{/get}}
        {{/get}}
  </section>

  <section class="agency-elections">
    <div class="gh-canvas">
      <div id="election-agency-app" data-agency="{{agency}}">
        <p>Agency election results load here</p>
      </div>
    </div>
  </section>
</main>
