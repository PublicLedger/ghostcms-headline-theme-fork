{{!< default}}

<main class="gh-main">
  <section class="gh-content gh-canvas">
    {{#get "pages" filter="slug:lookup-agency-{{agency}}" limit="1"}}
            {{#foreach pages}}
                {{{content}}}
            {{/foreach}}
        {{else}}
            {{#get "pages" filter="slug:lookup-agency" limit="1"}}
      
      
      
      
                {{#foreach pages}}
                    {{{content}}}
                {{/foreach}}




    {{else}}
      <h1>Lookup - {{agency}}</h1>
      <p>View officials for this agency.</p>
      <p>
        <em
          >Configure by creating a Page with slug: <code>lookup-agency</code> or
          <code>lookup-agency-{{agency}}</code></em
        >
      </p>
    {{/get}}
        {{/get}}
  </section>

  <section class="agency-lookup">
    <div class="gh-canvas">
      <div id="agency-lookup-app" data-agency="{{agency}}">
        <p>Agency-specific lookup loads here</p>
      </div>
    </div>
  </section>
</main>
