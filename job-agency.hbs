{{!< default}}

<main class="gh-main">
  <section class="gh-content gh-canvas">
    {{#get "pages" filter="slug:job-agency-{{agency}}" limit="1"}}
            {{#foreach pages}}
                {{{content}}}
            {{/foreach}}
        {{else}}
            {{#get "pages" filter="slug:job-agency" limit="1"}}
      
      
      
      
                {{#foreach pages}}
                    {{{content}}}
                {{/foreach}}




    {{else}}
      <h1>Jobs - {{agency}}</h1>
      <p>All elected positions in this agency.</p>
      <p>
        <em>Configure by creating a Page with slug: <code>job-agency</code> or <code>job-agency-{{agency}}</code></em>
      </p>
    {{/get}}
        {{/get}}
  </section>

  <section class="agency-jobs">
    <div class="gh-canvas">
      <div id="job-agency-app" data-agency="{{agency}}">
        <p>Agency jobs list loads here</p>
      </div>
    </div>
  </section>
</main>
