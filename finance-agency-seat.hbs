{{!< default}}

<main class="gh-main">
  <section class="gh-content gh-canvas">
    {{#get "pages" filter="slug:finance-agency-seat-{{agency}}-{{seat}}" limit="1"}}
            {{#foreach pages}}
                {{{content}}}
            {{/foreach}}
        {{else}}
            {{#get "pages" filter="slug:finance-agency-seat" limit="1"}}
      
      
      
      
                {{#foreach pages}}
                    {{{content}}}
                {{/foreach}}




    {{else}}
      <h1>Campaign Finance - {{seat}} ({{agency}})</h1>
      <p>Contributions and spending for this race.</p>
      <p>
        <em
          >Configure by creating a Page with slug: <code>finance-agency-seat</code> or
          <code>finance-agency-seat-{{agency}}-{{seat}}</code></em
        >
      </p>
    {{/get}}
        {{/get}}
  </section>

  <section class="seat-finance">
    <div class="gh-canvas">
      <div id="finance-seat-app" data-agency="{{agency}}" data-seat="{{seat}}">
        <p>Campaign finance data loads here (top donors, spending, candidate reports)</p>
      </div>
    </div>
  </section>
</main>
